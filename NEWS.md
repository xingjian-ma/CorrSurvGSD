# CorrSurvGSD 0.1.0.9000

- Align test files with the current package module and function names.
- Centralize test fixtures for the complete `validate_trial_input()` interface.
- Add a public `run_pipeline()` smoke test for release-candidate validation.
